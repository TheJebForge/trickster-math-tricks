```json
{
  "title": "Common Tricks",
  "icon": "minecraft:repeater",
  "category": "trickster-math-tricks:math-tricks"
}
```

This section contains tricks that might be similar to Trickster's existing tricks,
but they support more types.

;;;;;

<|trick@trickster-math-tricks:templates|trick-id=trickster-math-tricks:extract_x|>

Returns the X component of the given fragment.

;;;;;

<|trick@trickster-math-tricks:templates|trick-id=trickster-math-tricks:extract_y|>

Returns the Y component of the given fragment.

;;;;;

<|trick@trickster-math-tricks:templates|trick-id=trickster-math-tricks:extract_z|>

Returns the Z component of the given fragment.

;;;;;

<|trick@trickster-math-tricks:templates|trick-id=trickster-math-tricks:extract_w|>

Returns the W component of the given fragment.

;;;;;

<|trick@trickster-math-tricks:templates|trick-id=trickster-math-tricks:normalize|>

Normalizes the given fragment to a length of one.

;;;;;

<|trick@trickster-math-tricks:templates|trick-id=trickster-math-tricks:magnitude|>

Returns the length of the given fragment.

;;;;;

<|trick@trickster-math-tricks:templates|trick-id=trickster-math-tricks:squared_magnitude|>

Returns the squared length of the given fragment.